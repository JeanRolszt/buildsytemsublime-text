{
  "cmd": ["xfce4-terminal -e 'bash -c \"gcc ${file_name} -ansi -Wall -o ${file_base_name} -lm && ./${file_base_name};echo;echo Press ENTER to exit; read line\"'"],
  "shell": true,
  "file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$",
  "working_dir": "${file_path}",
  "selector": "source.c"    
}
